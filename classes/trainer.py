from typing import List
import uuid
from classes.types import Types

class Trainer:

    def __init__(self, id, name, pokemon:List[str]): 
        self.id = id
        self.name = name
        pokemon = pokemon

    def battle(user, user_team):
        Types.get_type()
